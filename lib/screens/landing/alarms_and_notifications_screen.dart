import 'package:flutter/material.dart';

class AlarmsAndNotificationsScreen extends StatelessWidget {
  const AlarmsAndNotificationsScreen({super.key});
  @override
  Widget build(BuildContext context) => Center(
    child: Text(
      "Alarms and Notification Screen",
      style: TextStyle(fontSize: 24),
    ),
  );
}
